apiVersion: v1
kind: Secret

metadata:
  name: vehicle-login-secret
  namespace: vehicle-login

type: Opaque

stringData:
  DATABASE_USER: "vehicle_admin"
  DATABASE_PASSWORD: "Vehicle@123"
  JWT_SECRET: "super-secret-jwt-key"
